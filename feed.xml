<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://zabardast.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://zabardast.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-24T22:30:04+00:00</updated><id>https://zabardast.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Making a Wifi6 wifi repeater with an ESP32-C6</title><link href="https://zabardast.github.io/blog/2026/esp32-wifi-repeater/" rel="alternate" type="text/html" title="Making a Wifi6 wifi repeater with an ESP32-C6"/><published>2026-05-24T15:25:00+00:00</published><updated>2026-05-24T15:25:00+00:00</updated><id>https://zabardast.github.io/blog/2026/esp32-wifi-repeater</id><content type="html" xml:base="https://zabardast.github.io/blog/2026/esp32-wifi-repeater/"><![CDATA[<p>Early validation log for my ESP32-C6 Wi-Fi repeater.</p> <h2 id="tests-run">Tests Run</h2> <ul> <li><code class="language-plaintext highlighter-rouge">STA</code> on home router</li> <li><code class="language-plaintext highlighter-rouge">AP</code> client connectivity and ping directionality</li> </ul> <h2 id="results">Results</h2> <h3 id="espsta-validation">espSTA validation:</h3> <ul> <li>ESP32-C6 connected to local router (<code class="language-plaintext highlighter-rouge">STA</code> mode). ✅</li> <li>Sent ping’s requests to another device on the same network. ✅</li> <li>Sent ping’s requests to another device on external network. ✅</li> <li>Responds to ping’s requests from local device. ✅</li> </ul> <p>In the tests above the <code class="language-plaintext highlighter-rouge">espSTA</code> is the only esp32 used for the tests it is always connected or communicating with phones, laptops or other standard devices.</p> <h4 id="sta-test-1">STA TEST 1:✅</h4> <p>can the <code class="language-plaintext highlighter-rouge">espSTA</code> ping other devices on the same network?</p> <ul> <li>The sta test 1.1 is the PC connected to local ap being pinged by my <code class="language-plaintext highlighter-rouge">espSTA</code>. <img src="/assets/img/posts/wifi_repeater/sta_tst_1.1.png" alt="test esp_sta 1.1" width="100%"/></li> <li>The sta test 1.2 is the <code class="language-plaintext highlighter-rouge">espSTA</code> connected to local ap returning a sucessful ping to 10.10.1.5(PC). <img src="/assets/img/posts/wifi_repeater/sta_tst_1.2.png" alt="test esp_sta 1.2" width="100%"/></li> </ul> <h4 id="sta-test-2">STA TEST 2:✅</h4> <p>can the <code class="language-plaintext highlighter-rouge">espSTA</code> be pinged by other devices on the same network? The sta test 2.1 is the PC connected to local ap pinging <code class="language-plaintext highlighter-rouge">espSTA</code>. <img src="/assets/img/posts/wifi_repeater/sta_tst_2.1.png" alt="test esp_sta 2.1" width="100%"/> The sta test 2.2 is the <code class="language-plaintext highlighter-rouge">espSTA</code> connected to local ap being pinged by PC. <img src="/assets/img/posts/wifi_repeater/sta_tst_2.2.png" alt="test esp_sta 2.2" width="100%"/></p> <h4 id="result">Result:</h4> <p>I can conclude that the <code class="language-plaintext highlighter-rouge">espSTA</code> can comfortably communicate with other devices on the same local network.</p> <h4 id="sta-test-3">STA TEST 3:✅</h4> <p>Now can it ping google? The <code class="language-plaintext highlighter-rouge">espSTA</code> test 3.1 is the esp32 pinging google and getting conclusive return packets that are translated by the log message success. <img src="/assets/img/posts/wifi_repeater/sta_tst_3.1.png" alt="test esp_sta 3.1" width="100%"/></p> <p>!!! git commit here !!! b647796fb104ae312be32c5e1c3f413b142fc0cb</p> <h3 id="espap-validation">espAP validation:</h3> <ul> <li>Station* connection to <code class="language-plaintext highlighter-rouge">espAP</code> *-&gt;(laptop, phone and <code class="language-plaintext highlighter-rouge">espSTA</code>) ✅</li> <li>Ping from laptop to phone using <code class="language-plaintext highlighter-rouge">espAP</code>. ✅</li> <li>Ping from <code class="language-plaintext highlighter-rouge">espSTA</code> to laptop. ✅</li> <li>Ping from laptop to <code class="language-plaintext highlighter-rouge">espSTA</code>. ✅</li> <li>Ping from <code class="language-plaintext highlighter-rouge">espSTA1</code> to <code class="language-plaintext highlighter-rouge">espSTA2</code>. ✅</li> </ul> <h4 id="ap-test-1">AP TEST 1:✅</h4> <p>Can I connect my phone to <code class="language-plaintext highlighter-rouge">espAP</code>? <img src="/assets/img/posts/wifi_repeater/ap_tst_1.1.png" alt="test esp_ap 1.1" width="100%"/> Here we can observe the <code class="language-plaintext highlighter-rouge">espAP</code> logging a connection from my phone <code class="language-plaintext highlighter-rouge">192.168.4.2</code> and proceding to receiving icmp request from it. I will proceed to trying to repeating the task with my laptop to see if I can catch the icmp request’s with wireshark. <img src="/assets/img/posts/wifi_repeater/ap_tst_1.2.png" alt="test esp_ap 1.2" width="100%"/> The ICMP(port unreachable) and ICMP type 03 leads me to think it is just residual apps on my laptop trying to reach out on the AP that is not yet connected to the internet so it cannot proceed to forwarding the packets there for replying with port unreachable.</p> <h4 id="ap-test-2">AP TEST 2:✅</h4> <p>I will now proceed with pings from and other device on the isolated <code class="language-plaintext highlighter-rouge">espAP</code> network. (Ping from phone(192.168.4.3) to laptop(192.168.4.2) using <code class="language-plaintext highlighter-rouge">espAP</code>.) <img src="/assets/img/posts/wifi_repeater/ap_tst_2.1.png" alt="test esp_ap 2.1" width="100%"/></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>For tests like this is use the app Vernet from Fdroid.
</code></pre></div></div> <h4 id="ap-test-3">AP TEST 3:✅</h4> <p>The Next challenge is to integrate a <code class="language-plaintext highlighter-rouge">espSTA</code> to se if they behave like other devices. Here we can see the log of the <code class="language-plaintext highlighter-rouge">espSTA</code> as it pings my phone(192.168.4.2) from 192.168.4.3. <img src="/assets/img/posts/wifi_repeater/ap_tst_3.1.png" alt="test esp_ap 3.1" width="100%"/></p> <p>Now lets try with the PC to see if we can actually catch the ICMP request emited by the esp32. Here we can see the captured requests on wireshark form the <code class="language-plaintext highlighter-rouge">espSTA</code> over the <code class="language-plaintext highlighter-rouge">espAP</code> to the PC. <img src="/assets/img/posts/wifi_repeater/ap_tst_3.2.png" alt="test esp_ap 3.2" width="100%"/></p> <h4 id="ap-test-4">AP TEST 4:✅</h4> <p>Ping <code class="language-plaintext highlighter-rouge">espSTA</code> from PC. <img src="/assets/img/posts/wifi_repeater/ap_tst_4.1.png" alt="test esp_ap 4.1" width="100%"/> So far so good.</p> <h4 id="ap-test-5">AP TEST 5:✅</h4> <p>Ping <code class="language-plaintext highlighter-rouge">espSTA</code>(1) from <code class="language-plaintext highlighter-rouge">espSTA</code>(2) through <code class="language-plaintext highlighter-rouge">espAP</code>. <img src="/assets/img/posts/wifi_repeater/ap_tst_5.1.png" alt="test esp_ap 5.1" width="100%"/></p> <h4 id="result-1">Result</h4> <p>I can comfortably say the STA and the AP part of this project work independently.</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">ESP_ERROR_CHECK</span><span class="p">(</span><span class="n">esp_wifi_set_mode</span><span class="p">(</span><span class="n">WIFI_MODE_AP</span><span class="p">));</span>
<span class="o">&amp;</span>
<span class="n">ESP_ERROR_CHECK</span><span class="p">(</span><span class="n">esp_wifi_set_mode</span><span class="p">(</span><span class="n">WIFI_MODE_STA</span><span class="p">));</span>
</code></pre></div></div> <h2 id="next-steps">Next Steps</h2> <ol> <li>explore WIFI_MODE_APSTA.</li> <li>NAT configuration.</li> </ol>]]></content><author><name></name></author><category term="default"/><category term="code"/><category term="automation"/><category term="web-development"/><summary type="html"><![CDATA[Initial steps towards creating a Wifi6 repeater using the ESP32-C6 module.]]></summary></entry><entry><title type="html">Automating item insertion in Squareup for my part time job</title><link href="https://zabardast.github.io/blog/2025/eastonbooks1/" rel="alternate" type="text/html" title="Automating item insertion in Squareup for my part time job"/><published>2025-03-26T14:24:00+00:00</published><updated>2025-03-26T14:24:00+00:00</updated><id>https://zabardast.github.io/blog/2025/eastonbooks1</id><content type="html" xml:base="https://zabardast.github.io/blog/2025/eastonbooks1/"><![CDATA[<h2 id="task-automating-the-naming-of-item-variations-in-squareup-for-my-part-time-job">Task: Automating the naming of item variations in Squareup for my part time job</h2> <h2 id="scope-of-the-work">Scope of the Work:</h2> <p>When adding used books to Square’s inventory system, every item requires a variation name to be manually entered. For a used bookstore where most items are single copies, this adds unnecessary friction to the data entry workflow.</p> <p>The goal: <strong>Automatically populate the variation name field with a standardized format</strong> (<code class="language-plaintext highlighter-rouge">Eastons Books - [Category]</code>) whenever I add a new book to inventory.</p> <h2 id="initial-assumptions">Initial Assumptions:</h2> <ul> <li>Square’s UI would render predictably enough to be reliably targeted by DOM selectors</li> <li>The category information would be available in the DOM before the variation name field needs to be populated</li> <li>A mutation observer approach would be responsive enough for real-time field population</li> </ul> <h2 id="problems-encountered">Problems Encountered:</h2> <p><strong>1. Timing &amp; DOM Detection</strong> Square’s interface is dynamically rendered. Fields don’t exist until they’re needed, and their appearance isn’t immediately predictable. I needed a way to detect when new fields were added to the DOM and react accordingly.</p> <p><strong>2. Extracting Category Information</strong> The category isn’t stored in a simple input field. It’s rendered as text inside a nested element structure (<code class="language-plaintext highlighter-rouge">[class="advanced-categorization__category-list"]</code>). I had to traverse the DOM to find the first child element and extract its text content.</p> <p><strong>3. The “Dirty” Field Problem</strong> Square’s form validation checks whether a field has been modified before allowing it to be saved. Simply setting the field’s value programmatically doesn’t trigger this “dirty” state, so the variation name wouldn’t be saved. This required manual intervention. I’d need to add or remove a character after the automatic insertion for it to save correctly.</p> <h2 id="design-decisions--tradeoffs">Design Decisions &amp; Tradeoffs:</h2> <p><strong>1. MutationObserver for Real-Time Detection</strong> I chose to use a <code class="language-plaintext highlighter-rouge">MutationObserver</code> that watches the entire document body for DOM changes (<code class="language-plaintext highlighter-rouge">childList: true, subtree: true</code>). This approach:</p> <ul> <li>Catches the moment new fields appear</li> <li>Works with Square’s dynamic rendering without needing to know the exact timing</li> <li>Trades performance for reliability (observing the entire body could be expensive, but the overhead is minimal for this use case)</li> </ul> <p><strong>2. Global Variable for State Management</strong> I used a module-level variable (<code class="language-plaintext highlighter-rouge">category_name</code>) to store the category between the two execution cycles:</p> <ul> <li>First cycle: Capture the category when it appears</li> <li>Second cycle: Insert it into the variation name field when that field appears</li> <li>This is simple but relies on the category always being set before the variation name field is rendered</li> <li>leads to edge cases if the user has multiple tabs open at the same time.</li> </ul> <p><strong>3. Living with the “Dirty” Field Limitation</strong> Rather than trying to force Square’s form validation to recognize programmatic changes (which could be fragile), I accepted the limitation as a feature: <strong>it forces me to review the auto-inserted text before saving</strong>, preventing accidental overwrites of existing variation names.</p> <h2 id="current-status">Current Status:</h2> <p>The plugin is actively used and reduces manual data entry by approximately 20%, enabling faster inventory processing with fewer errors.</p> <p>The implementation works reliably in production, though it requires one manual action per entry (adding/removing a character to mark the field as “dirty” and trigger save validation).</p> <h2 id="next-steps">Next Steps:</h2> <p>Future enhancements could include:</p> <ul> <li><strong>Automatic field validation</strong>: Trigger Square’s form dirty-state detection programmatically (e.g., via <code class="language-plaintext highlighter-rouge">dispatchEvent</code>) instead of relying on manual intervention</li> <li><strong>SKU automation</strong>: Extract SKU numbers and populate them in the item field of the label printing page for easy label generation</li> </ul> <h2 id="implementation-highlights">Implementation Highlights:</h2> <p>The core logic is straightforward:</p> <ol> <li><strong>MutationObserver</strong> listens for all DOM changes</li> <li><strong>On each mutation</strong>, run <code class="language-plaintext highlighter-rouge">applyScript()</code>: <ul> <li>If the variation name field exists and hasn’t been filled yet, insert the category</li> <li>If the category hasn’t been captured yet, extract it from the DOM</li> </ul> </li> <li><strong>State management</strong> via a global variable ensures the category persists across multiple script executions</li> </ol> <p>This demonstrates a practical approach to browser automation: sometimes the simplest solution (observing the entire DOM and reacting to changes) is more reliable than trying to predict exact timing or hook into specific lifecycle events.</p> <h2 id="code-snippet">Code Snippet:</h2> <div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
<span class="kd">var</span> <span class="nx">category_name</span> <span class="o">=</span> <span class="dl">''</span><span class="p">;</span>

<span class="kd">function</span> <span class="nf">applyScript</span><span class="p">()</span> <span class="p">{</span>

  <span class="c1">// add prefix</span>
  <span class="kd">const</span> <span class="nx">emberActionElement</span> <span class="o">=</span> <span class="nb">document</span><span class="p">.</span><span class="nf">querySelector</span><span class="p">(</span><span class="dl">'</span><span class="s1">[data-test-variation-name=""]</span><span class="dl">'</span><span class="p">);</span>
  <span class="k">if </span><span class="p">(</span><span class="nx">emberActionElement</span> <span class="o">&amp;&amp;</span> <span class="nx">emberActionElement</span><span class="p">.</span><span class="nf">getAttribute</span><span class="p">(</span><span class="dl">'</span><span class="s1">value</span><span class="dl">'</span><span class="p">)</span> <span class="o">==</span> <span class="dl">'</span><span class="s1">Regular</span><span class="dl">'</span><span class="p">)</span> <span class="p">{</span>
    <span class="nx">console</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="dl">'</span><span class="s1">set value</span><span class="dl">'</span><span class="p">);</span>
    <span class="k">if </span><span class="p">(</span><span class="nx">category_name</span><span class="p">)</span> <span class="p">{</span>
      <span class="nx">emberActionElement</span><span class="p">.</span><span class="nf">setAttribute</span><span class="p">(</span><span class="dl">'</span><span class="s1">value</span><span class="dl">'</span><span class="p">,</span> <span class="dl">'</span><span class="s1">Eastons Books - </span><span class="dl">'</span> <span class="o">+</span> <span class="nx">category_name</span><span class="p">);</span>
      <span class="nx">category_name</span> <span class="o">=</span> <span class="dl">''</span><span class="p">;</span>
    <span class="p">}</span>
  <span class="p">}</span>

  <span class="c1">// add category</span>
  <span class="k">if</span><span class="p">(</span> <span class="nx">category_name</span> <span class="o">==</span> <span class="dl">''</span><span class="p">)</span> <span class="p">{</span>
    <span class="kd">const</span> <span class="nx">categoryElement</span> <span class="o">=</span> <span class="nb">document</span><span class="p">.</span><span class="nf">querySelector</span><span class="p">(</span><span class="dl">'</span><span class="s1">[class="advanced-categorization__category-list"]</span><span class="dl">'</span><span class="p">);</span>
    <span class="k">if </span><span class="p">(</span><span class="nx">categoryElement</span><span class="p">)</span> <span class="p">{</span>
        <span class="nx">category_name</span> <span class="o">=</span> <span class="nx">categoryElement</span><span class="p">.</span><span class="nx">firstElementChild</span><span class="p">.</span><span class="nx">textContent</span><span class="p">;</span>
        <span class="nx">console</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="dl">'</span><span class="s1">category set to: </span><span class="dl">'</span> <span class="o">+</span> <span class="nx">category_name</span><span class="p">);</span>
    <span class="p">}</span>
  <span class="p">}</span>
<span class="p">}</span>

<span class="nf">applyScript</span><span class="p">();</span>

<span class="kd">const</span> <span class="nx">observer</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">MutationObserver</span><span class="p">((</span><span class="nx">mutations</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">console</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="dl">'</span><span class="s1">DOM changed. Reapplying script...</span><span class="dl">'</span><span class="p">);</span>
  <span class="nf">applyScript</span><span class="p">();</span>
<span class="p">});</span>

<span class="nx">observer</span><span class="p">.</span><span class="nf">observe</span><span class="p">(</span><span class="nb">document</span><span class="p">.</span><span class="nx">body</span><span class="p">,</span> <span class="p">{</span>
  <span class="na">childList</span><span class="p">:</span> <span class="kc">true</span><span class="p">,</span>
  <span class="na">subtree</span><span class="p">:</span> <span class="kc">true</span>
<span class="p">});</span>

</code></pre></div></div> <p><a href="https://github.com/MolleWare/Squareup_auto_variation_naming">github-repo</a></p>]]></content><author><name></name></author><category term="default"/><category term="code"/><category term="automation"/><category term="web-development"/><summary type="html"><![CDATA[description]]></summary></entry><entry><title type="html">Updating Squareup auto variation name generation after website update.</title><link href="https://zabardast.github.io/blog/2025/eastonbooks1_update/" rel="alternate" type="text/html" title="Updating Squareup auto variation name generation after website update."/><published>2025-03-26T14:24:00+00:00</published><updated>2025-03-26T14:24:00+00:00</updated><id>https://zabardast.github.io/blog/2025/eastonbooks1_update</id><content type="html" xml:base="https://zabardast.github.io/blog/2025/eastonbooks1_update/"><![CDATA[<h2 id="situation-">Situation :</h2> <p>This monday the squarup website had new updates related to new ways of inserting items on their platform. This update resulting in changing the structure of the DOM and consequently breaking my plugin.</p> <h2 id="task-">Task :</h2> <p>The objective of this step is 1 to fix the plugin. 2 to document my process to hopefully help me with this step next time I or someone else needs to do it.</p> <h2 id="initial-steps-">Initial steps :</h2> <p>Look what symbols have been removed and are blocking the good process of my plugin. <code class="language-plaintext highlighter-rouge">data-test-variation-name=""</code> : this was the symbol I was using to locate the input I would insert the variation name in. <code class="language-plaintext highlighter-rouge">[class="advanced-categorization__category-list"]'</code> : this one was for grabing the variation name. Both of these symbols where removed in this new version forcing me to find new ways of grabing and inserting the data.</p> <p><strong>side note:</strong> I was still verry interested in keeping the “Dirty Field” issue because it actually saved time when mis clicking we would not delete the input. and we could still make changes without changing the variation name.</p> <h3 id="additional-information">Additional information</h3> <p>In the querySelector the use of “[]” allows me to grab any unique part of the html file. That way I am not dependent of a Class or an Id.</p> <h3 id="research-of-new-symbols">Research of new symbols</h3> <p>[I will add more info on how I found them after writing myself notes at work]. Get variation name was <code class="language-plaintext highlighter-rouge">placeholder="Such as a size"</code>. and to write the new variation name I used this <code class="language-plaintext highlighter-rouge">class="_name_d8c7fc7"</code>.</p> <h3 id="minor-changes-in-the-structure-of-the-section-used-to-read-category-name">Minor changes in the structure of the section used to read category name.</h3> <p>The category name was no longer in a span so I removed <code class="language-plaintext highlighter-rouge">.firstElementChild</code>.</p> <h2 id="new-code-snippet">New Code Snippet:</h2> <div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">var</span> <span class="nx">category_name</span> <span class="o">=</span> <span class="dl">''</span><span class="p">;</span>

<span class="c1">// Function to apply your script</span>
<span class="kd">function</span> <span class="nf">applyScript</span><span class="p">()</span> <span class="p">{</span>

  <span class="c1">// add prefix</span>
  <span class="kd">const</span> <span class="nx">emberActionElement</span> <span class="o">=</span> <span class="nb">document</span><span class="p">.</span><span class="nf">querySelector</span><span class="p">(</span><span class="dl">'</span><span class="s1">[placeholder="Such as a size"]</span><span class="dl">'</span><span class="p">);</span>
  <span class="k">if </span><span class="p">(</span><span class="nx">emberActionElement</span><span class="p">)</span> <span class="p">{</span>
    <span class="nx">console</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="dl">'</span><span class="s1">set value</span><span class="dl">'</span><span class="p">);</span>
    <span class="k">if </span><span class="p">(</span><span class="nx">category_name</span><span class="p">)</span> <span class="p">{</span>
      <span class="nx">emberActionElement</span><span class="p">.</span><span class="nf">setAttribute</span><span class="p">(</span><span class="dl">'</span><span class="s1">value</span><span class="dl">'</span><span class="p">,</span> <span class="dl">'</span><span class="s1">Eastons Books - </span><span class="dl">'</span> <span class="o">+</span> <span class="nx">category_name</span><span class="p">);</span>
      <span class="nx">category_name</span> <span class="o">=</span> <span class="dl">''</span><span class="p">;</span>
    <span class="p">}</span>
  <span class="p">}</span>

  <span class="c1">// add category</span>
  <span class="k">if</span><span class="p">(</span> <span class="nx">category_name</span> <span class="o">==</span> <span class="dl">''</span><span class="p">)</span> <span class="p">{</span>
    <span class="kd">const</span> <span class="nx">categoryElement</span> <span class="o">=</span> <span class="nb">document</span><span class="p">.</span><span class="nf">querySelector</span><span class="p">(</span><span class="dl">'</span><span class="s1">[class="_name_d8c7fc7"]</span><span class="dl">'</span><span class="p">);</span>
    <span class="k">if </span><span class="p">(</span><span class="nx">categoryElement</span><span class="p">)</span> <span class="p">{</span>
        <span class="nx">category_name</span> <span class="o">=</span> <span class="nx">categoryElement</span><span class="p">.</span><span class="nx">textContent</span><span class="p">;</span>
        <span class="nx">console</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="dl">'</span><span class="s1">category set to: </span><span class="dl">'</span> <span class="o">+</span> <span class="nx">category_name</span><span class="p">);</span>
    <span class="p">}</span>
  <span class="p">}</span>
<span class="p">}</span>

<span class="c1">// Run the script initially</span>
<span class="nf">applyScript</span><span class="p">();</span>

<span class="c1">// Set up a MutationObserver to re-run the script when the DOM changes</span>
<span class="kd">const</span> <span class="nx">observer</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">MutationObserver</span><span class="p">((</span><span class="nx">mutations</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">console</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="dl">'</span><span class="s1">DOM changed. Reapplying script...</span><span class="dl">'</span><span class="p">);</span>
  <span class="nf">applyScript</span><span class="p">();</span>
<span class="p">});</span>

<span class="c1">// Start observing the document body for changes</span>
<span class="nx">observer</span><span class="p">.</span><span class="nf">observe</span><span class="p">(</span><span class="nb">document</span><span class="p">.</span><span class="nx">body</span><span class="p">,</span> <span class="p">{</span>
  <span class="na">childList</span><span class="p">:</span> <span class="kc">true</span><span class="p">,</span>    <span class="c1">// Watch for added/removed elements</span>
  <span class="na">subtree</span><span class="p">:</span> <span class="kc">true</span>       <span class="c1">// Watch all descendants</span>
<span class="p">});</span>

</code></pre></div></div> <p><a href="https://github.com/MolleWare/Squareup_auto_variation_naming">github-repo</a></p>]]></content><author><name></name></author><category term="default"/><category term="code"/><category term="automation"/><category term="web-development"/><summary type="html"><![CDATA[description]]></summary></entry></feed>